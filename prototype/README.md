# Prototype

This directory holds your functioning Proof of Concept and its associated implementation material. Reorganize this directory as needed once your project's technical requirements become clear (e.g. splitting into `/src`, `/hardware`, `/build`, etc.).
